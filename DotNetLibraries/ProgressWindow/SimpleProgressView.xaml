<Window x:Class="ProgressWindow.SimpleProgressView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProgressWindow"
        mc:Ignorable="d" 
        Title=" 进度" Height="130" Width="400"
        WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="{Binding MainText}" Margin="10" FontFamily="PingFangSC-Regular" FontSize="14"/>
            <ProgressBar x:Name="progressBar" Height="30" Margin="10" IsIndeterminate="True" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
